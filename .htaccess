DirectoryIndex index.php

# Block direct access to SQLite database files (defense in depth).
<FilesMatch "\.(sqlite|sqlite3)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>

